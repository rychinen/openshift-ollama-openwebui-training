apiVersion: v1
kind: Service
metadata:
  name: ollama
spec:
  selector:
    app: ollama-incorrect
  ports:
  - name: http
    port: 11434
    targetPort: http
# Recover: oc apply -f manifests/02-ollama.yaml
